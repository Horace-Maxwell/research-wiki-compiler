# Screenshot Placeholder Plan

Screenshots are intentionally not committed yet.

Capture these before the first public or semi-public release:

1. `dashboard-overview.png`
   Show workspace counts, recent local activity, and workspace posture.
2. `wiki-browser.png`
   Show the three-pane wiki browser with page tree, rendered Markdown, and metadata.
3. `sources-detail.png`
   Show imported source detail with normalization, chunks, and summary artifacts.
4. `review-queue.png`
   Show a proposal detail with rationale, risk level, diff, and status controls.
5. `ask-grounded-answer.png`
   Show a grounded answer with citations, based-on pages, and archive controls.
6. `audit-browser.png`
   Show audit history and at least one concrete finding detail.
7. `settings-provider-config.png`
   Show provider configuration without exposing real credentials.

Guidance:

- use the seeded demo workspace
- avoid local machine usernames or personal directories in the capture when possible
- mask or blur provider keys and any sensitive local paths
