# Optional app logging / 可选日志级别
LOG_LEVEL=info

# Optional live showcase settings / 可选 live showcase 配置
# Reference mode does not need an API key. / reference 模式不需要 API key。
OPENAI_API_KEY=
OPENCLAW_EXAMPLE_LIVE_PROVIDER=openai
OPENCLAW_EXAMPLE_LIVE_MODEL=gpt-5.4
